PROCEDURE PR_INIT_BY_DEPT IS
BEGIN
  SET_BLOCK_PROPERTY('EMPLOYEES', ONETIME_WHERE, 'DEPARTMENT_ID=' || :DEPARTMENTS.DEPARTMENT_ID);
  GO_BLOCK('EMPLOYEES');
  EXECUTE_QUERY;
  GO_BLOCK('DEPARTMENTS');
END;